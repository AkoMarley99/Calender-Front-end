import React from "react";

export default function ReminderHeader (props) {
    return (
        <div>
            <button>Display previous date</button>

            <div>Display currrent date</div>

            <button>Display the next days date </button>

        </div>
    )
}